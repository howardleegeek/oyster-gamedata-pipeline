# Distributed Bug-Hunt Findings — 2026-05-04

> Triggered by Howard "分布式找bug" + "反复做测试". Parallel inspection of
> shipped sample tarball v0.1.0-rc9 across 5 files + release page UX.

## Findings (6 bugs)

| # | Severity | Component | Bug | Status |
|---|---|---|---|---|
| 1 | **CRITICAL** | `depth/*.exr` | Files are 16×16 all-zeros, not 1920×1080 with real Z values. Synthesizer wrote sentinel placeholders. Buyer would reject. | OPEN |
| 2 | **CRITICAL** | `systeminfo.json` | Has internal keys (gameProcessName, x, y, width, height, recordDpi, map_scale, map_bounds), zero PRD-required keys (gpu, cpu, ram_gb, os, build). Adapter normalization needed. | OPEN |
| 3 | **HIGH** | Release notes | PowerShell bootstrap had `cd ...\tmp_install` but x64 CI zip extracts flat — vendors hit "path not found" on copy-paste | **FIXED** in release-notes update |
| 4 | MEDIUM | Release notes | Asset sizes mismatched: 45→43.2 MB, 27→26.7 MB | **FIXED** (rounded to 43/27) |
| 5 | MEDIUM | `action_camera.json` | Field `'camera_Follow Offset'` has space + capital F. Should be `camera_follow_offset` | OPEN |
| 6 | MEDIUM | `action_camera.json` | Mixed naming: `keyCode` (camelCase) + `route_type` (snake_case). Pick one convention. | OPEN |

## What still passes (don't fix what's not broken)

- ✅ `video.mp4` — H.264 1920×1080 30fps 300s 9000 frames (matches PRD exactly)
- ✅ `action_camera.json` — 9000 records, 20 fields, Vector3 list format
- ✅ `gameinfo.xlsx` — `GameInfo` sheet, 2 rows × 6 cols
- ✅ Release page: download links work, SmartScreen disclosure clear, PowerShell ASCII-clean
- ✅ Recorder boot: 10/10 deterministic, 349-361 ms init through OBS

## Methodology

7 parallel inspections (ffprobe / json schema / xlsx structure / OpenEXR
header / file PE arch / release-page WebFetch / curl HEAD on download URL)
plus 1 QATester subagent for browser-driven release-page validation.

Total wall-clock cost: <2 minutes. Cluster equivalent would have taken
hours per probe; parallelism collapsed it.

## Next recommended specs (cluster targets)

| Proposed spec | Title |
|---|---|
| G161 | `bin/sample_tarball_real_depth.py` — replace 16×16 sentinel with real 1920×1080 Z-buffer in synthesizer |
| G162 | `src/oyster_agent_runner/systeminfo_adapter.py` — normalize recorder keys to PRD-required keys |
| G163 | `bin/audit_field_naming_consistency.py` — enforce snake_case across action_camera schema |
| G164 | `bin/exr_quality_lint.py` — lint that rejects EXR with constant-valued Z (catches future stub regressions) |
| G165 | `bin/systeminfo_required_keys_lint.py` — enforce PRD-required keys present |
