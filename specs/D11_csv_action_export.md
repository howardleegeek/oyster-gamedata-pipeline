# D11 — action_camera.json → CSV exporter
Implement `bin/action_camera_to_csv.py action_camera.json`. Flattens 20-field per-frame records to CSV for Excel/Pandas viewing. Real columns only. Pure stdlib csv module. Tests: 5-record JSON → 5-row CSV.
