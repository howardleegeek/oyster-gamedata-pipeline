#!/usr/bin/env python3
"""
spec_generator.py — Safe YAML-aware spec appender for audit_gaps.yaml.

Why this exists:
    A previous string-concat append broke the YAML at line 447 col 22, halting the harness.
    This generator builds Python dicts and uses yaml.safe_dump → cannot produce invalid YAML.

Usage:
    python3 bin/spec_generator.py            # dry-run, prints what would be added
    python3 bin/spec_generator.py --apply    # actually write + validate
"""

from __future__ import annotations

import sys
from pathlib import Path

import yaml

REPO = Path(__file__).resolve().parent.parent
GAPS_FILE = REPO / "docs" / "audit_gaps.yaml"


# ---------------------------------------------------------------------------
# 100 atomic NEW-FILE specs across 9 wave categories.
# ---------------------------------------------------------------------------
SPECS: list[dict] = [
    # ─── W9 Edge cases (15) ───────────────────────────────────────────────
    {"id": "G039", "title": "bin/edge_test_zero_records.py",
     "purpose": "Boundary test: action_camera.json with empty records list — adapter must fail-closed not crash",
     "lines": 80, "priority": "P1"},
    {"id": "G040", "title": "bin/edge_test_negative_timestamps.py",
     "purpose": "Boundary test: timestamp 1970-01-01 negative epoch — schema must reject pre-2020 explicitly",
     "lines": 90, "priority": "P1"},
    {"id": "G041", "title": "bin/edge_test_gigantic_record_count.py",
     "purpose": "Boundary test: 1,000,000 records in single action_camera.json — confirm adapter streams not loads-all",
     "lines": 110, "priority": "P1"},
    {"id": "G042", "title": "bin/edge_test_unicode_filenames.py",
     "purpose": "Boundary test: Chinese / emoji / RTL filenames in tarball entries — verify utf-8 manifest support",
     "lines": 100, "priority": "P2"},
    {"id": "G043", "title": "bin/edge_test_missing_optional_fields.py",
     "purpose": "Boundary test: skip optional Vector4 quat field — adapter must default-fill not crash",
     "lines": 95, "priority": "P1"},
    {"id": "G044", "title": "bin/edge_test_extra_unknown_fields.py",
     "purpose": "Boundary test: vendor adds extra keys to action_camera record — lint warns but accepts",
     "lines": 90, "priority": "P2"},
    {"id": "G045", "title": "bin/edge_test_nan_inf_floats.py",
     "purpose": "Boundary test: NaN / +Inf in Vector3 — lint must reject as buyer spec requires finite floats",
     "lines": 105, "priority": "P0"},
    {"id": "G046", "title": "bin/edge_test_empty_strings.py",
     "purpose": "Boundary test: empty string for required string field (route_type) — fail-closed",
     "lines": 80, "priority": "P1"},
    {"id": "G047", "title": "bin/edge_test_max_int_values.py",
     "purpose": "Boundary test: int64 max for frame_id — confirm no overflow in adapter math",
     "lines": 85, "priority": "P2"},
    {"id": "G048", "title": "bin/edge_test_min_int_values.py",
     "purpose": "Boundary test: int64 min for frame_id — confirm no underflow",
     "lines": 85, "priority": "P2"},
    {"id": "G049", "title": "bin/edge_test_dst_clock_change.py",
     "purpose": "Boundary test: capture spans DST transition — UTC timestamps must remain monotonic",
     "lines": 100, "priority": "P1"},
    {"id": "G050", "title": "bin/edge_test_leap_second.py",
     "purpose": "Boundary test: leap-second insertion at 23:59:60 — adapter handles or rejects cleanly",
     "lines": 95, "priority": "P3"},
    {"id": "G051", "title": "bin/edge_test_high_precision_floats.py",
     "purpose": "Boundary test: 1e-300 tiny floats in camera position — verify json round-trip preserves precision",
     "lines": 90, "priority": "P2"},
    {"id": "G052", "title": "bin/edge_test_quaternion_norm_drift.py",
     "purpose": "Boundary test: quaternion magnitude 1.0001 from float drift — lint tolerates within epsilon",
     "lines": 110, "priority": "P1"},
    {"id": "G053", "title": "bin/edge_test_camera_pitch_singularity.py",
     "purpose": "Boundary test: pitch exactly 90.0 / -90.0 gimbal-lock case — adapter clamps not wraps",
     "lines": 100, "priority": "P0"},

    # ─── W10 Stress / load (5) ────────────────────────────────────────────
    {"id": "G054", "title": "bin/stress_test_50_concurrent_lints.py",
     "purpose": "Stress test: spawn 50 lint processes against same tarball — verify no shared-state corruption",
     "lines": 130, "priority": "P1"},
    {"id": "G055", "title": "bin/stress_test_huge_tarball_5gb.py",
     "purpose": "Stress test: build 5 GB tarball (long capture + 6fps depth) — verify upload_s3.sh chunked path holds",
     "lines": 140, "priority": "P1"},
    {"id": "G056", "title": "bin/stress_test_long_capture_2h.py",
     "purpose": "Stress test: simulate 2-hour scene with 30-min cap — confirm scene_id rotation and clip cap enforced",
     "lines": 150, "priority": "P1"},
    {"id": "G057", "title": "bin/stress_test_burst_50_clips.py",
     "purpose": "Stress test: 50 clips per minute burst — lint queue must drain without deadlock",
     "lines": 120, "priority": "P2"},
    {"id": "G058", "title": "bin/stress_test_memory_leak_check.py",
     "purpose": "Stress test: 1000-iteration adapter run with tracemalloc — assert RSS growth less than 50MB",
     "lines": 130, "priority": "P1"},

    # ─── W11 Vendor scenarios (10) ────────────────────────────────────────
    {"id": "G059", "title": "bin/vendor_scenario_first_clip.py",
     "purpose": "Walkthrough: vendor follows README from zero, uploads first clip — measure time-to-first-clip metric",
     "lines": 140, "priority": "P0"},
    {"id": "G060", "title": "bin/vendor_scenario_china_mirror.py",
     "purpose": "Walkthrough: vendor in CN behind GFW — pip mirror + S3 region selection works without VPN",
     "lines": 130, "priority": "P1"},
    {"id": "G061", "title": "bin/vendor_scenario_resume_after_crash.py",
     "purpose": "Walkthrough: kill capture mid-clip, restart — confirm no partial tarball poisons s3 + manifest detects gap",
     "lines": 150, "priority": "P0"},
    {"id": "G062", "title": "bin/vendor_scenario_low_bandwidth.py",
     "purpose": "Walkthrough: 200 Kbps upload — chunked retry succeeds within 30 min budget",
     "lines": 130, "priority": "P1"},
    {"id": "G063", "title": "bin/vendor_scenario_low_disk.py",
     "purpose": "Walkthrough: 1 GB free disk — capture pre-flight check warns and aborts safely",
     "lines": 110, "priority": "P1"},
    {"id": "G064", "title": "bin/vendor_scenario_mac_only.py",
     "purpose": "Walkthrough: vendor on M1 Mac no GPU — depth provider falls back to CPU model",
     "lines": 120, "priority": "P2"},
    {"id": "G065", "title": "bin/vendor_scenario_old_python_310.py",
     "purpose": "Walkthrough: vendor on Ubuntu 22.04 default Python 3.10 — adapter runs without datetime.UTC",
     "lines": 100, "priority": "P0"},
    {"id": "G066", "title": "bin/vendor_scenario_no_gpu.py",
     "purpose": "Walkthrough: no CUDA / Metal — DepthAnything inference falls back to onnx-cpu within SLA",
     "lines": 130, "priority": "P1"},
    {"id": "G067", "title": "bin/vendor_scenario_alpha_week.py",
     "purpose": "Walkthrough: alpha-week first 50 vendors — concurrent ingest + per-vendor quota holds",
     "lines": 140, "priority": "P1"},
    {"id": "G068", "title": "bin/vendor_scenario_rejection_loop.py",
     "purpose": "Walkthrough: vendor uploads malformed clip — rejection email + retry guide reaches them",
     "lines": 130, "priority": "P1"},

    # ─── W12 PRD-deep coverage (15) ───────────────────────────────────────
    {"id": "G069", "title": "bin/prd_test_video_no_ui.py",
     "purpose": "PRD p4 #3: video must contain no overlay UI / chat / dialogs — OCR scan asserts clean frames",
     "lines": 140, "priority": "P0"},
    {"id": "G070", "title": "bin/prd_test_route_type_distribution.py",
     "purpose": "PRD p5 #2: route_type field across 240 clips must hit at least 5 distinct types — distribution check",
     "lines": 110, "priority": "P1"},
    {"id": "G071", "title": "bin/prd_test_wasd_balance.py",
     "purpose": "PRD p6 #4: WASD action balance — no single key over 60 percent in long captures",
     "lines": 100, "priority": "P1"},
    {"id": "G072", "title": "bin/prd_test_stationary_threshold.py",
     "purpose": "PRD p6 #5: stationary frames over 5s must trigger clip stop — verify cutoff",
     "lines": 110, "priority": "P0"},
    {"id": "G073", "title": "bin/prd_test_action_per_second.py",
     "purpose": "PRD p6 #6: median actions-per-second 0.5 to 5.0 — out-of-band capture flagged as low-quality",
     "lines": 100, "priority": "P1"},
    {"id": "G074", "title": "bin/prd_test_camera_intrinsics_pinhole.py",
     "purpose": "PRD p3 #2: camera projection must be pinhole — fov + aspect populated, no fisheye distortion params",
     "lines": 120, "priority": "P0"},
    {"id": "G075", "title": "bin/prd_test_left_hand_coordinates.py",
     "purpose": "PRD p3 #4: left-hand coordinate system — assert handedness via cross-product sign on Vector3 axes",
     "lines": 100, "priority": "P0"},
    {"id": "G076", "title": "bin/prd_test_metric_units_meters.py",
     "purpose": "PRD p3 #5: positions in meters — sanity-bound camera_position within world cube radius",
     "lines": 95, "priority": "P1"},
    {"id": "G077", "title": "bin/prd_test_speed_units_mps.py",
     "purpose": "PRD p3 #6: linear_velocity in m/s — bound to player walk run sprint speeds",
     "lines": 100, "priority": "P1"},
    {"id": "G078", "title": "bin/prd_test_240_clip_cap.py",
     "purpose": "PRD p7 #2: max 240 clips per scene — adapter stops at 241st",
     "lines": 90, "priority": "P0"},
    {"id": "G079", "title": "bin/prd_test_30min_scene_cap.py",
     "purpose": "PRD p7 #3: max 30 min per scene — clock cap enforced",
     "lines": 95, "priority": "P0"},
    {"id": "G080", "title": "bin/prd_test_systeminfo_required.py",
     "purpose": "PRD p7 file 1: required keys (gpu, cpu, ram_gb, os, build) all present — fail-closed if missing",
     "lines": 100, "priority": "P0"},
    {"id": "G081", "title": "bin/prd_test_depth_invalid_marker.py",
     "purpose": "PRD p4 #6: depth invalid pixel sentinel value (zero or NaN) preserved through OpenEXR roundtrip",
     "lines": 110, "priority": "P0"},
    {"id": "G082", "title": "bin/prd_test_depth_6fps_alignment.py",
     "purpose": "PRD p4 #5: depth EXR 6fps alignment with 30fps video — frame index ratio 5:1 exact",
     "lines": 105, "priority": "P0"},
    {"id": "G083", "title": "bin/prd_test_audio_continuity.py",
     "purpose": "PRD p4 #2: video audio track continuous (no gaps over 50ms) — ffprobe + numpy diff check",
     "lines": 120, "priority": "P1"},

    # ─── W13 Red team adversarial (15) ────────────────────────────────────
    {"id": "G084", "title": "bin/red_team_oversized_json.py",
     "purpose": "Red team: 100 MB action_camera.json — confirm adapter rejects or streams without OOM",
     "lines": 110, "priority": "P0"},
    {"id": "G085", "title": "bin/red_team_nan_coordinates.py",
     "purpose": "Red team: inject NaN into camera_position — lint must reject not silently propagate",
     "lines": 90, "priority": "P0"},
    {"id": "G086", "title": "bin/red_team_year_9999_timestamp.py",
     "purpose": "Red team: timestamp year 9999 — schema clamps within sane window",
     "lines": 85, "priority": "P1"},
    {"id": "G087", "title": "bin/red_team_path_traversal.py",
     "purpose": "Red team: tarball entry name contains ../../etc — extractor must refuse traversal",
     "lines": 100, "priority": "P0"},
    {"id": "G088", "title": "bin/red_team_duplicate_frame_id.py",
     "purpose": "Red team: two records with same frame_id — lint detects dupe and rejects",
     "lines": 95, "priority": "P1"},
    {"id": "G089", "title": "bin/red_team_concurrent_writers.py",
     "purpose": "Red team: two adapter procs write same tarball simultaneously — file lock prevents corruption",
     "lines": 130, "priority": "P0"},
    {"id": "G090", "title": "bin/red_team_sigkill_mid_write.py",
     "purpose": "Red team: SIGKILL adapter mid action_camera write — atomic temp+rename leaves no half-file",
     "lines": 120, "priority": "P0"},
    {"id": "G091", "title": "bin/red_team_disk_full.py",
     "purpose": "Red team: simulate ENOSPC during write — adapter aborts cleanly with clear error",
     "lines": 110, "priority": "P0"},
    {"id": "G092", "title": "bin/red_team_out_of_order_frames.py",
     "purpose": "Red team: shuffle frame ordering in JSON — lint rejects non-monotonic frame_id sequence",
     "lines": 90, "priority": "P1"},
    {"id": "G093", "title": "bin/red_team_wrong_obs_key.py",
     "purpose": "Red team: WebSocket auth with wrong password — session refuses + audit log records attempt",
     "lines": 100, "priority": "P1"},
    {"id": "G094", "title": "bin/red_team_corrupt_exr.py",
     "purpose": "Red team: zero-fill 1 KB inside EXR — validator detects corruption via numpy isnan/isinf scan",
     "lines": 110, "priority": "P0"},
    {"id": "G095", "title": "bin/red_team_invalid_systeminfo.py",
     "purpose": "Red team: systeminfo.json missing required gpu key — lint v2 fails closed",
     "lines": 80, "priority": "P0"},
    {"id": "G096", "title": "bin/red_team_wrong_fps.py",
     "purpose": "Red team: video tagged 60fps — lint rejects (PRD requires exactly 30fps)",
     "lines": 90, "priority": "P0"},
    {"id": "G097", "title": "bin/red_team_mixed_vector_format.py",
     "purpose": "Red team: same file mixes dict and list Vector3 forms — lint rejects format inconsistency",
     "lines": 100, "priority": "P1"},
    {"id": "G098", "title": "bin/red_team_clock_skew.py",
     "purpose": "Red team: capture machine clock jumps backward 1 hour — adapter switches to monotonic clock",
     "lines": 110, "priority": "P0"},

    # ─── W14 Blue team defense (paired hardening) (15) ────────────────────
    {"id": "G099", "title": "src/oyster_agent_runner/defense_size_limit.py",
     "purpose": "Blue team for G084: per-file size cap defender (10 MB action_camera, 500 MB video)",
     "lines": 110, "priority": "P0"},
    {"id": "G100", "title": "src/oyster_agent_runner/defense_finite_check.py",
     "purpose": "Blue team for G085: assert_finite helper running on every Vector3 value before write",
     "lines": 90, "priority": "P0"},
    {"id": "G101", "title": "src/oyster_agent_runner/defense_timestamp_range.py",
     "purpose": "Blue team for G086: enforce timestamps within 2024-01-01 to 2030-01-01 range",
     "lines": 80, "priority": "P1"},
    {"id": "G102", "title": "src/oyster_agent_runner/defense_path_sanitize.py",
     "purpose": "Blue team for G087: tarball path sanitizer rejecting absolute / traversal members",
     "lines": 100, "priority": "P0"},
    {"id": "G103", "title": "src/oyster_agent_runner/defense_dedup_frames.py",
     "purpose": "Blue team for G088: in-memory set tracker rejecting duplicate frame_id within scene",
     "lines": 90, "priority": "P1"},
    {"id": "G104", "title": "src/oyster_agent_runner/defense_file_lock.py",
     "purpose": "Blue team for G089: fcntl flock wrapper around tarball open — prevents concurrent corruption",
     "lines": 110, "priority": "P0"},
    {"id": "G105", "title": "src/oyster_agent_runner/defense_atomic_write.py",
     "purpose": "Blue team for G090: write_atomic helper using tempfile + os.replace",
     "lines": 95, "priority": "P0"},
    {"id": "G106", "title": "src/oyster_agent_runner/defense_disk_check.py",
     "purpose": "Blue team for G091: pre-flight shutil.disk_usage check requiring 5 GB free before capture",
     "lines": 90, "priority": "P0"},
    {"id": "G107", "title": "src/oyster_agent_runner/defense_frame_order.py",
     "purpose": "Blue team for G092: streaming validator asserting frame_id strictly increases by 1",
     "lines": 100, "priority": "P1"},
    {"id": "G108", "title": "src/oyster_agent_runner/defense_obs_auth.py",
     "purpose": "Blue team for G093: SHA256+base64 OBS auth helper with rate-limit on failed attempts",
     "lines": 110, "priority": "P1"},
    {"id": "G109", "title": "src/oyster_agent_runner/defense_exr_validate.py",
     "purpose": "Blue team for G094: post-write EXR validator scanning for NaN clusters and shape mismatch",
     "lines": 120, "priority": "P0"},
    {"id": "G110", "title": "src/oyster_agent_runner/defense_systeminfo_required.py",
     "purpose": "Blue team for G095: systeminfo schema with REQUIRED_KEYS list and pydantic-lite validator",
     "lines": 100, "priority": "P0"},
    {"id": "G111", "title": "src/oyster_agent_runner/defense_fps_strict.py",
     "purpose": "Blue team for G096: ffprobe wrapper asserting r_frame_rate exactly 30/1",
     "lines": 90, "priority": "P0"},
    {"id": "G112", "title": "src/oyster_agent_runner/defense_vector_uniform.py",
     "purpose": "Blue team for G097: scan first record format and enforce uniform Vector3 shape across file",
     "lines": 100, "priority": "P1"},
    {"id": "G113", "title": "src/oyster_agent_runner/defense_monotonic_clock.py",
     "purpose": "Blue team for G098: prefer time.monotonic_ns inside capture loop, wall clock only for stamp",
     "lines": 100, "priority": "P0"},

    # ─── W15 Autoresearch bench (10) ──────────────────────────────────────
    {"id": "G114", "title": "bin/autoresearch_lint_perf.py",
     "purpose": "Autoresearch: benchmark lint_buyer_spec on 100-tarball corpus — surface p50 / p95 / p99",
     "lines": 130, "priority": "P1"},
    {"id": "G115", "title": "bin/autoresearch_adapter_quality.py",
     "purpose": "Autoresearch: golden-corpus diff vs hand-labeled mineflayer scenes — coverage and recall metrics",
     "lines": 150, "priority": "P1"},
    {"id": "G116", "title": "bin/autoresearch_compression_ratio.py",
     "purpose": "Autoresearch: compare H.264 vs H.265 vs AV1 sizes on same scene — recommend codec",
     "lines": 130, "priority": "P2"},
    {"id": "G117", "title": "bin/autoresearch_depth_quality.py",
     "purpose": "Autoresearch: DepthAnything V2 vs Marigold MAE on 50 ground-truth Minecraft Z-buffer frames",
     "lines": 160, "priority": "P1"},
    {"id": "G118", "title": "bin/autoresearch_failure_modes.py",
     "purpose": "Autoresearch: enumerate top 10 lint failure modes from 1000 vendor tarballs — drives spec backlog",
     "lines": 140, "priority": "P1"},
    {"id": "G119", "title": "bin/autoresearch_recovery_time.py",
     "purpose": "Autoresearch: kill-9 then restart adapter — measure mean time to first new clip after crash",
     "lines": 110, "priority": "P1"},
    {"id": "G120", "title": "bin/autoresearch_throughput.py",
     "purpose": "Autoresearch: clips-per-vendor-per-day at 50 / 200 / 1000 vendors — capacity planning",
     "lines": 130, "priority": "P1"},
    {"id": "G121", "title": "bin/autoresearch_clip_density.py",
     "purpose": "Autoresearch: action density across scene types (combat / build / explore) — diversity metric",
     "lines": 120, "priority": "P2"},
    {"id": "G122", "title": "bin/autoresearch_data_diversity.py",
     "purpose": "Autoresearch: biome / time-of-day / weather distribution per 1000 clips — flag undersampled",
     "lines": 130, "priority": "P2"},
    {"id": "G123", "title": "bin/autoresearch_action_entropy.py",
     "purpose": "Autoresearch: Shannon entropy of action stream — low entropy suggests AFK / scripted vendor",
     "lines": 110, "priority": "P1"},

    # ─── W16 Production hardening (10) ────────────────────────────────────
    {"id": "G124", "title": "bin/observability_metrics_emitter.py",
     "purpose": "Production: prometheus-style counters / histograms emitted to stdout for adapter / lint / upload",
     "lines": 140, "priority": "P1"},
    {"id": "G125", "title": "bin/health_check_endpoint.py",
     "purpose": "Production: thin HTTP server reporting last_clip_at / disk_free / queue_depth — for ops",
     "lines": 120, "priority": "P1"},
    {"id": "G126", "title": "bin/audit_log_writer.py",
     "purpose": "Production: append-only newline-delimited JSON audit log of every capture / lint / upload",
     "lines": 110, "priority": "P0"},
    {"id": "G127", "title": "bin/idempotency_token.py",
     "purpose": "Production: per-clip uuid with at-least-once dedup on backend ingest path",
     "lines": 100, "priority": "P0"},
    {"id": "G128", "title": "bin/circuit_breaker.py",
     "purpose": "Production: trip after N consecutive S3 failures — halt uploads and alert",
     "lines": 130, "priority": "P1"},
    {"id": "G129", "title": "bin/rate_limiter.py",
     "purpose": "Production: token-bucket per vendor key — enforce per-day clip budget on adapter side",
     "lines": 110, "priority": "P1"},
    {"id": "G130", "title": "bin/graceful_shutdown.py",
     "purpose": "Production: SIGTERM handler flushes in-flight writes + closes tarball before exit",
     "lines": 100, "priority": "P0"},
    {"id": "G131", "title": "bin/recovery_orchestrator.py",
     "purpose": "Production: on startup, scan staging dir for half-baked tarballs and either resume or quarantine",
     "lines": 140, "priority": "P0"},
    {"id": "G132", "title": "bin/secret_rotator.py",
     "purpose": "Production: 90-day automatic S3 key rotation with overlap window — zero-downtime",
     "lines": 130, "priority": "P1"},
    {"id": "G133", "title": "bin/dependency_pinning_check.py",
     "purpose": "Production: assert all pip deps pinned to exact version — fail CI if any range",
     "lines": 90, "priority": "P1"},

    # ─── W17 Customer polish (5) ──────────────────────────────────────────
    {"id": "G134", "title": "bin/installer_one_click.py",
     "purpose": "Customer: single bash script that detects OS / installs deps / sets up PATH for vendors",
     "lines": 150, "priority": "P0"},
    {"id": "G135", "title": "bin/error_message_translator.py",
     "purpose": "Customer: convert internal exception traces into vendor-friendly remediation message",
     "lines": 120, "priority": "P1"},
    {"id": "G136", "title": "bin/onboarding_smoke_test.py",
     "purpose": "Customer: post-install verify — captures 10s clip and lints, reports go / no-go",
     "lines": 130, "priority": "P0"},
    {"id": "G137", "title": "bin/uninstall_clean.py",
     "purpose": "Customer: remove all installed files and config — leaves zero trace including launchd plists",
     "lines": 110, "priority": "P2"},
    {"id": "G138", "title": "bin/diag_bundle_collector.py",
     "purpose": "Customer: gather logs / systeminfo / last 3 manifests into a tarball for support tickets",
     "lines": 130, "priority": "P1"},

    # ─── W18 Research-driven PRD optimization (22 specs) ───────────────────
    # Source: docs/PRD_OPTIMIZATION_PROPOSAL.md (synthesis of 3 research agents).
    # Cluster A — drop-in compat with OXE / RLDS / VPT / Octo
    {"id": "G139", "title": "src/oyster_agent_runner/buyer_spec_v2_camera_intrinsics.py",
     "purpose": "Cluster A: per-frame K matrix + distortion + T_world_cam SE3 pose (Ego-Exo4D / nuScenes / DROID standard) — without K depth EXR is uninterpretable in 3D",
     "lines": 200, "priority": "P0"},
    {"id": "G140", "title": "src/oyster_agent_runner/buyer_spec_v2_rlds_flags.py",
     "purpose": "Cluster A: is_first / is_last / is_terminal episode-boundary flags per record (Open X-Embodiment / RT-X mandatory) — unlocks OXE pooling",
     "lines": 130, "priority": "P0"},
    {"id": "G141", "title": "src/oyster_agent_runner/buyer_spec_v2_language_instruction.py",
     "purpose": "Cluster A: per-episode instruction string + optional dense narration (RT-2 / OpenVLA / Octo / Pi-0 require it) — unlocks VLA fine-tune ecosystem",
     "lines": 150, "priority": "P0"},
    {"id": "G142", "title": "src/oyster_agent_runner/buyer_spec_v2_vpt_mouse.py",
     "purpose": "Cluster A: extend mouse with scaledX/Y + dwheel + buttons bitmask + newButtons edge-triggered (OpenAI VPT / MineWorld schema) — drop-in VPT compat",
     "lines": 170, "priority": "P1"},
    {"id": "G143", "title": "src/oyster_agent_runner/buyer_spec_v2_action_tokens.py",
     "purpose": "Cluster A: 256-bin per-dim discretization of action vector (RT-1 / RT-2 actions-as-language) — table-stakes for VLA training",
     "lines": 180, "priority": "P1"},
    {"id": "G144", "title": "src/oyster_agent_runner/buyer_spec_v2_action_chunks.py",
     "purpose": "Cluster A: action_chunk_next_4 + action_chunk_next_10 packing for diffusion-decoded policies (Octo / OpenVLA / Pi-0)",
     "lines": 150, "priority": "P2"},
    # Cluster B — legal / compliance (EU AI Act + CA AB 2013 effective 2026)
    {"id": "G145", "title": "bin/c2pa_signer.py",
     "purpose": "Cluster B: C2PA v2.1 signed manifest with AI/ML assertion + is_synthetic flag — mandatory under EU AI Act Aug 2026 + CA AB 2013 Jan 2026",
     "lines": 220, "priority": "P0"},
    {"id": "G146", "title": "src/oyster_agent_runner/hmac_machine_id.py",
     "purpose": "Cluster B: HMAC-rotated machine fingerprint replacing raw MAC/disk-serial hash — GDPR personal-data avoidance per EDPB 2024 guidance",
     "lines": 100, "priority": "P0"},
    {"id": "G147", "title": "src/oyster_agent_runner/privacy_kanon_inputs.py",
     "purpose": "Cluster B: k-anonymity bucketing on mouse/key inter-event intervals to defeat keystroke-dynamics re-identification (~99% re-id literature)",
     "lines": 110, "priority": "P1"},
    {"id": "G148", "title": "bin/synthetic_disclosure_metadata.py",
     "purpose": "Cluster B: ensure is_synthetic + engine + engine_version + capture_date in every JSON / manifest / video sidecar (synthetic-content disclosure mandate)",
     "lines": 80, "priority": "P0"},
    # Cluster C — sensor fidelity enrichment
    {"id": "G149", "title": "bin/imu_provider.py",
     "purpose": "Cluster C: synthetic IMU 6-axis (3-accel + 3-gyro) at 240Hz physics-tick rate from Mineflayer state (Ego-Exo4D / VIO papers — derived velocity is lossy)",
     "lines": 250, "priority": "P1"},
    {"id": "G150", "title": "bin/seg_track_provider.py",
     "purpose": "Cluster C: per-pixel semantic + instance segmentation at 6fps (R=class_id from Minecraft block IDs, G+B=instance_id, CARLA / Habitat convention)",
     "lines": 280, "priority": "P1"},
    {"id": "G151", "title": "src/oyster_agent_runner/depth_uint16_alt_writer.py",
     "purpose": "Cluster C: alternative uint16 PNG mm depth output alongside float32 EXR (ScanNet++ / Habitat convention, 4x smaller)",
     "lines": 130, "priority": "P2"},
    {"id": "G152", "title": "bin/reward_signal_provider.py",
     "purpose": "Cluster C: per-step sparse reward float + per-episode task_success bool (DreamerV3-family world-model / RL training requirement)",
     "lines": 160, "priority": "P2"},
    {"id": "G153", "title": "bin/embodiment_metadata.py",
     "purpose": "Cluster C: per-scene embodiment.json with embodiment_id + agent_geometry + locomotion_mode (arxiv 2505.05753 scaling-laws axis)",
     "lines": 100, "priority": "P2"},
    {"id": "G154", "title": "bin/inventory_voxel_capture.py",
     "purpose": "Cluster C: per-frame inventory + 3x3x3 voxel-window block-IDs around player (MineDojo / MineWorld multimodal observation)",
     "lines": 230, "priority": "P2"},
    # Cluster D — format / packaging compat
    {"id": "G155", "title": "bin/parquet_manifest_writer.py",
     "purpose": "Cluster D: gameinfo.parquet + action_camera.parquet shards keyed by (clip_id, frame_idx) replacing xlsx (AV2 / OXE / DROID convention)",
     "lines": 180, "priority": "P1"},
    {"id": "G156", "title": "bin/rlds_export.py",
     "purpose": "Cluster D: convert one tarball into TFDS RLDS-format shard (tf.data.Dataset compatible) — unlocks OXE pooling",
     "lines": 280, "priority": "P1"},
    {"id": "G157", "title": "bin/lerobot_export.py",
     "purpose": "Cluster D: convert tarball to LeRobot HuggingFace format — HF-hub distribution path for robotics buyers",
     "lines": 110, "priority": "P2"},
    {"id": "G158", "title": "bin/hdf5_episode_pack.py",
     "purpose": "Cluster D: optional episode.h5 packing all action / depth / seg / imu in one file (BEHAVIOR-1K / OmniGibson pattern)",
     "lines": 180, "priority": "P3"},
    # Cluster E — data hygiene scoring
    {"id": "G159", "title": "bin/aesthetic_scorer.py",
     "purpose": "Cluster E: per-clip aesthetic / motion / OCR-overlay / camera-jitter scores (Open-Sora 2.0 filter stack) — buyer reweighting / curation",
     "lines": 320, "priority": "P2"},
    {"id": "G160", "title": "bin/data_diversity_dashboard.py",
     "purpose": "Cluster E: per-cohort diversity dashboard (route_type / biome / time-of-day / action-entropy histograms) for buyer pre-purchase verification",
     "lines": 220, "priority": "P2"},
    # ─── W19 PRD-grounded fixes from full Lark spec audit (5 specs) ────────
    # Source: docs/PRD_AUDIT_2026_05_04.md (synthesis from full 14-page PDF read)
    {"id": "G161", "title": "bin/synthesize_real_depth.py",
     "purpose": "Cluster A: 1920x1080 float32 single-channel EXR depth synthesis with realistic Z-buffer (distance-to-plane) — replaces 16x16 all-zeros placeholder; cap invalid-pixel ratio at 0.1 percent",
     "lines": 200, "priority": "P0"},
    {"id": "G162", "title": "bin/synthesize_route_diversity.py",
     "purpose": "Cluster A: route_type distribution 50 percent normal + 50 percent special/loop; WASD distribution W=40 percent / A=S=D=20 percent each — replaces 100 percent route_type=1 mono-distribution",
     "lines": 150, "priority": "P0"},
    {"id": "G163", "title": "src/oyster_agent_runner/keycode_int_normalizer.py",
     "purpose": "Cluster A: convert list-form keyCode arrays to single int per record per PRD page 11 spec (W=87 ASCII), or expand list into multiple frame records",
     "lines": 80, "priority": "P0"},
    {"id": "G164", "title": "src/oyster_agent_runner/intrinsics_capital_keys.py",
     "purpose": "Cluster A: rename camera_intrinsics dict keys from lowercase fx/fy/cx/cy to PRD-specified fx/fy/Cx/Cy (capital Cx Cy) per p5",
     "lines": 60, "priority": "P0"},
    {"id": "G165", "title": "bin/lint_v3_prd_grounded.py",
     "purpose": "Cluster A: full PRD page-by-page lint — checks all 24 acceptance criteria including audio quality / route distribution / camera_intrinsics fx==fy / quaternion xyzw order / depth invalid-pixel ratio / keyCode int format / 5-6 min duration / 1920x1080 strict / no UI overlay / no logo / no popup",
     "lines": 350, "priority": "P0"},
    # ─── W20 Buyer-eye-popping production scenarios (10 specs) ─────────────
    # Goal: cover MORE than competitors (OWL Control / MineRL / Habitat).
    # Derived from PRD_OPTIMIZATION_PROPOSAL.md Cluster C/D/E + new scenarios.
    {"id": "G166", "title": "bin/multi_camera_capture.py",
     "purpose": "Cluster C+: multi-camera concurrent capture (1st-person + 3rd-person + top-down) — Habitat-Sim parity for buyers training multi-view world models",
     "lines": 280, "priority": "P0"},
    {"id": "G167", "title": "bin/optical_flow_provider.py",
     "purpose": "Cluster E+: per-frame optical flow computation (RAFT model, 2D vectors) — GAIA-2 / Sora-class buyers want flow-conditioned generation",
     "lines": 320, "priority": "P1"},
    {"id": "G168", "title": "bin/per_frame_object_bbox.py",
     "purpose": "Cluster E+: per-frame 2D + 3D bounding boxes for visible NPCs/items (CARLA / nuScenes parity)",
     "lines": 250, "priority": "P1"},
    {"id": "G169", "title": "bin/material_albedo_provider.py",
     "purpose": "Cluster C+: per-pixel albedo + roughness + metallic G-buffer (Sora 2 / Cosmos training data parity)",
     "lines": 220, "priority": "P2"},
    {"id": "G170", "title": "bin/scene_lighting_metadata.py",
     "purpose": "Cluster C+: per-frame sun direction + ambient intensity + weather state (allows relighting experiments)",
     "lines": 180, "priority": "P1"},
    {"id": "G171", "title": "bin/temporal_consistency_lint.py",
     "purpose": "Lint v4: detect temporal artifacts (frame-to-frame discontinuity > 30 percent in flow) — buyers reject jitter",
     "lines": 200, "priority": "P0"},
    {"id": "G172", "title": "bin/automatic_diversity_metric.py",
     "purpose": "Cluster E+: per-scene diversity score (biome / time / weather entropy) so buyer can sort cohorts",
     "lines": 230, "priority": "P1"},
    {"id": "G173", "title": "bin/audio_event_track.py",
     "purpose": "PRD p8 audio req: per-frame audio peak + event-classifier (footstep / ambient / etc.) — addresses earlier PRD audio gap",
     "lines": 270, "priority": "P0"},
    {"id": "G174", "title": "bin/replay_determinism_tester.py",
     "purpose": "Production hardening: re-run same seed twice, diff frame-by-frame, assert byte-identical — proves data is deterministic",
     "lines": 250, "priority": "P0"},
    {"id": "G175", "title": "bin/buyer_dashboard_html.py",
     "purpose": "Customer polish: static HTML report (no server) summarizing 1 clip — preview frames + lint result + diversity stats — buyer eye-pop",
     "lines": 350, "priority": "P0"},

    # ─── W21 Multi-game horizontal expansion (8 specs) ─────────────────────
    {"id": "G176", "title": "src/oyster_agent_runner/environments/beamng_drive.py",
     "purpose": "BeamNG.drive extractor: BeamNGpy connect + Vehicle.poll_sensors + Camera depth-mode + ego pose at 60Hz",
     "lines": 350, "priority": "P0"},
    {"id": "G177", "title": "src/oyster_agent_runner/environments/stardew_valley.py",
     "purpose": "Stardew Valley extractor: SMAPI mod relay over HTTP/JSON socket; player position/facing + map name + action keys at 60Hz",
     "lines": 280, "priority": "P0"},
    {"id": "G178", "title": "src/oyster_agent_runner/environments/cyberpunk_2077.py",
     "purpose": "CP2077 extractor: CET (Cyber Engine Tweaks) Lua over websocket; 6-DoF camera + player pose + velocity + FOV + day-night",
     "lines": 380, "priority": "P0"},
    {"id": "G179", "title": "src/oyster_agent_runner/environments/cities_skylines.py",
     "purpose": "Cities Skylines extractor: Mod API + Harmony patch shim; camera position 3D + zoom + simulation tick over named pipe",
     "lines": 300, "priority": "P0"},
    {"id": "G180", "title": "src/oyster_agent_runner/environments/factorio_full.py",
     "purpose": "Factorio extractor v2: full RCON + observer-mod loop; player pose + facing + tile state + biter wave events",
     "lines": 320, "priority": "P0"},
    {"id": "G181", "title": "src/oyster_agent_runner/environments/registry.py",
     "purpose": "Environment plugin registry: discovers environments/*.py modules + uniform AgentTask -> Environment factory",
     "lines": 180, "priority": "P0"},
    {"id": "G182", "title": "bin/cross_game_test_harness.py",
     "purpose": "Cross-game integration test harness: enumerates registered environments + runs 30s mock trajectory + lints; gates new game integrations",
     "lines": 250, "priority": "P0"},
    {"id": "G183", "title": "docs/runbooks/STARDEW_RUNBOOK.md",
     "purpose": "Operator runbook for Stardew capture: SMAPI install + our mod copy + 30-min smoke; mirrors BEAMNG_RUNBOOK",
     "lines": 180, "priority": "P1"},

    # ─── W22 GitHub Actions repair (6 NEW-FILE specs) ──────────────────────
    # Howard: '我看见 GitHub action 一直失败 集群 修复一下'
    # CI failing because: heartbeat spam triggering 86/100 runs, cluster
    # output unformatted, test mock data bugs, no CI health visibility.
    {"id": "G184", "title": "bin/git_hook_heartbeat_skip_ci.sh",
     "purpose": "Pre-commit hook script that detects heartbeat-only audit_gaps.yaml diffs and appends [skip ci] to the commit message; eliminates 86 percent of CI workflow runs",
     "lines": 90, "priority": "P0"},
    {"id": "G185", "title": "bin/cluster_output_autoformat.py",
     "purpose": "Pre-commit auto-formatter for cluster-shipped code; runs black + ruff --fix on the staged file before harness commit, catching the unformatted-output failure mode",
     "lines": 130, "priority": "P0"},
    {"id": "G186", "title": "tests/bin/conftest_rcon_helpers.py",
     "purpose": "Shared pytest fixtures for RCON packet construction; replaces magic numbers like length=14 with helper rcon_pack_response(id, type, body) that computes correct length",
     "lines": 110, "priority": "P0"},
    {"id": "G187", "title": "bin/ci_health_dashboard.py",
     "purpose": "CLI that polls gh run list, computes pass/fail rate over last N runs, surfaces top-3 failure patterns; observability for the harness operator",
     "lines": 200, "priority": "P0"},
    {"id": "G188", "title": "bin/auto_fix_ci_failures.py",
     "purpose": "Reads gh run view --log-failed, applies known fixes (black --check failure -> run black; ruff failure -> ruff --fix; missing import -> add it); commits with auto-fix-ci tag",
     "lines": 280, "priority": "P0"},
    {"id": "G189", "title": ".github/workflows/heartbeat_skip_check.yml",
     "purpose": "Workflow that runs on every push and short-circuits with skip-marker output if commit message contains [skip ci] or paths-ignore patterns; redundancy layer for paths-ignore",
     "lines": 60, "priority": "P1"},

    # ─── W23 Production completeness (12 NEW-FILE specs) ───────────────────
    # Howard: 'Claude token 不多了, 尽量用集群推进'.
    # P0 product gaps: backend, portal, payments, audio, real depth, anomaly,
    # multi-clip stitcher, telemetry, runbooks.
    {"id": "G190", "title": "bin/backend_ingest_handler.py",
     "purpose": "FastAPI endpoint /v1/ingest accepts vendor tarball uploads, validates via G165 lint, stores to S3, writes Postgres clip row with vendor_id + duration + sha256",
     "lines": 320, "priority": "P0"},
    {"id": "G191", "title": "bin/s3_presigned_url_issuer.py",
     "purpose": "Issue 24h presigned S3 PUT URLs to vendors (one URL per clip); rate-limit per vendor key; write upload-intent row in Postgres for audit",
     "lines": 200, "priority": "P0"},
    {"id": "G192", "title": "bin/vendor_portal_static_site.py",
     "purpose": "Generator for static SPA (HTML + JS, no server) — vendor signs in via OAuth, sees earnings + clips uploaded + recorder download link",
     "lines": 380, "priority": "P0"},
    {"id": "G193", "title": "bin/payout_calculator.py",
     "purpose": "Compute vendor payout: clip_duration_min * rate_per_min * quality_multiplier (from G165 lint score) - rejection_penalties; outputs CSV for ACH",
     "lines": 220, "priority": "P0"},
    {"id": "G194", "title": "bin/anomaly_detector_clip_quality.py",
     "purpose": "Flag low-effort vendor submissions: action entropy < threshold OR camera variance < threshold OR identical-trajectory pattern across N clips (catches farming)",
     "lines": 280, "priority": "P0"},
    {"id": "G195", "title": "bin/buyer_download_api_handler.py",
     "purpose": "FastAPI /v1/buyer/clips endpoint: paginated clip list + presigned download URL per clip; auth via buyer JWT; rate-limit + audit log",
     "lines": 250, "priority": "P0"},
    {"id": "G196", "title": "bin/audio_track_extractor.py",
     "purpose": "Extract audio from video.mp4 via ffmpeg, validate continuity (no gaps over 50ms per PRD p8), check volume/distortion; emit audio.flac + audio_qc.json",
     "lines": 230, "priority": "P0"},
    {"id": "G197", "title": "bin/multi_clip_stitcher.py",
     "purpose": "Combine N short same-scene clips into longer episode; preserve timestamp continuity + frame_id offset; for buyers wanting longer trajectories",
     "lines": 260, "priority": "P1"},
    {"id": "G198", "title": "bin/depth_shader_pack_minecraft.py",
     "purpose": "Generator for Iris/Sodium shader pack that exports Z-buffer at 6fps to disk; ground-truth depth replaces DepthAnything inference for MC capture",
     "lines": 350, "priority": "P0"},
    {"id": "G199", "title": "bin/recorder_health_telemetry.py",
     "purpose": "Vendor recorder posts heartbeat (uptime, FPS, encoder backpressure, last-clip-at) to /v1/health endpoint every 60s; backend dashboard surfaces stragglers",
     "lines": 200, "priority": "P1"},
    {"id": "G200", "title": "docs/runbooks/BACKEND_DEPLOY.md",
     "purpose": "Deploy FastAPI backend to Render or Railway: env vars + Postgres URL + S3 creds + alembic migrate + healthcheck verification",
     "lines": 220, "priority": "P0"},
    {"id": "G201", "title": "docs/runbooks/PAYMENT_SETUP.md",
     "purpose": "Vendor payout setup: Stripe Connect for US + manual ACH/wire spreadsheet for non-US; tax form (W-9 / W-8BEN) collection runbook",
     "lines": 200, "priority": "P1"},

    # ─── W24 Launch-readiness (12 NEW-FILE specs) ──────────────────────────
    # Howard: 继续推进, 集群干, claude token 省着用.
    {"id": "G202", "title": "bin/vendor_signup_flow.py",
     "purpose": "Vendor signup: email + Google OAuth + KYC-light (full name + payout method) + recorder download URL emit; writes vendors row in Postgres",
     "lines": 280, "priority": "P0"},
    {"id": "G203", "title": "bin/buyer_signup_flow.py",
     "purpose": "Buyer signup: company info + sales contact + JWT issuance + sample-clip download grant; writes buyers row + audit",
     "lines": 240, "priority": "P0"},
    {"id": "G204", "title": "docs/runbooks/VENDOR_INSTALL_VALIDATOR.md",
     "purpose": "Validator runbook for vendor PCs: GPU model + driver + Java + Node + disk free + network speed checklist before recorder.exe runs",
     "lines": 200, "priority": "P0"},
    {"id": "G205", "title": "bin/backup_orchestrator.py",
     "purpose": "Daily backup: pg_dump Postgres + S3 cross-region replication + Glacier 90-day retention; reports last-success timestamp",
     "lines": 280, "priority": "P0"},
    {"id": "G206", "title": "bin/cn_vendor_mirror.py",
     "purpose": "CN-side mirror: Aliyun OSS bucket + presigned URL issuer for vendors behind GFW; bypasses S3 latency without VPN",
     "lines": 220, "priority": "P1"},
    {"id": "G207", "title": "bin/dr_failover_runbook_check.py",
     "purpose": "Disaster recovery validation: simulate primary-region outage, verify failover region serves /v1/ingest within 60s, restores last-good Postgres backup",
     "lines": 250, "priority": "P1"},
    {"id": "G208", "title": "sdk/python/oyster_buyer_sdk/__init__.py",
     "purpose": "Python SDK for buyers: list_clips() / download_clip() / stream_dataset() with progress bars + presigned URL refresh; pip-installable",
     "lines": 320, "priority": "P0"},
    {"id": "G209", "title": "sdk/javascript/oyster-buyer/index.js",
     "purpose": "JavaScript / TypeScript SDK for buyers: same API as Python SDK; for Node.js training-pipeline integration",
     "lines": 280, "priority": "P1"},
    {"id": "G210", "title": "bin/buyer_evaluation_harness.py",
     "purpose": "Reference benchmark: train tiny world model on N clips of our data, evaluate on held-out test set; emits MSE / SSIM / FID for buyer to verify data quality before bulk purchase",
     "lines": 350, "priority": "P0"},
    {"id": "G211", "title": "docs/MARKETING_SITE_COPY.md",
     "purpose": "Landing page copy: hero (play games get paid) + 3-step how-it-works + buyer pitch + 5-FAQ + CTA",
     "lines": 200, "priority": "P1"},
    {"id": "G212", "title": "docs/TOS_VENDOR.md",
     "purpose": "Vendor terms of service: data ownership transfer + payout schedule + anti-cheat compliance clause + termination + IP indemnity (no copyrighted-mod data)",
     "lines": 250, "priority": "P0"},
    {"id": "G213", "title": "docs/PRIVACY_POLICY.md",
     "purpose": "Privacy policy: vendor PII (email, payout info) + clip data retention 90 days + GDPR DSAR procedure + California CCPA opt-out + deletion endpoint",
     "lines": 280, "priority": "P0"},

    # ─── W25 Consumer-facing pivot (14 NEW-FILE specs) ─────────────────────
    # Howard 2026-05-05: 'final product 面向 游戏爱好者和消费者 直接装我们软件'.
    # Pivots from B2B vendor pipeline to B2C gamer install. See
    # docs/CONSUMER_PIVOT_STRATEGY.md for what stays / what re-frames.
    {"id": "G214", "title": "bin/installer_one_click_windows.py",
     "purpose": "Windows .msi installer: bundles Java 21 + recorder.exe + auto-launch-on-boot reg key + uninstaller; one click for consumer (no pip, no terminal)",
     "lines": 320, "priority": "P0"},
    {"id": "G215", "title": "bin/installer_macos_pkg.py",
     "purpose": "macOS .pkg installer for Mac gamers: bundles Java 21 + recorder + LaunchAgent plist; signed with Developer ID per Apple notarization",
     "lines": 280, "priority": "P0"},
    {"id": "G216", "title": "bin/onboarding_consumer_splash.py",
     "purpose": "First-run splash screen v1: age gate (COPPA 13+) + region (GDPR/CCPA) + per-game opt-in checkboxes + Continue button. NO email or payout collection in v1 (local-capture-only MVP)",
     "lines": 260, "priority": "P0"},
    {"id": "G217", "title": "bin/game_auto_detector.py",
     "purpose": "Windows / macOS process-list scanner: detects when supported games launch (Minecraft.exe, BeamNG.drive.exe, Stardew Valley.exe, Cyberpunk2077.exe, etc.); fires hooks",
     "lines": 280, "priority": "P0"},
    {"id": "G218", "title": "bin/auto_record_orchestrator.py",
     "purpose": "Auto-record state machine: game-launch event -> start recording, game-exit event -> stop + queue upload, network-online event -> drain queue; consumer never touches anything",
     "lines": 320, "priority": "P0"},
    {"id": "G219", "title": "bin/system_tray_consumer_ui.py",
     "purpose": "Native system tray UI (cross-platform via pystray + tkinter): icon shows status (idle / recording / uploading / earned $X today), right-click menu (settings, history, exit)",
     "lines": 360, "priority": "P0"},
    {"id": "G220", "title": "bin/consumer_privacy_dashboard.py",
     "purpose": "In-app privacy dashboard: per-game opt-in toggles + 'what we record' explainer + 'delete my data' button (calls /v1/user/delete) + GDPR DSAR shortcut",
     "lines": 280, "priority": "P0"},
    {"id": "G221", "title": "bin/consent_log_signed.py",
     "purpose": "Legally-binding signed consent log: HMAC-signed entry per recording session capturing user_id + game + timestamp + opt-in version; serves GDPR/CCPA/COPPA",
     "lines": 220, "priority": "P0"},
    {"id": "G222", "title": "bin/paypal_micropayout_handler.py",
     "purpose": "PayPal Payouts API for consumer micro-payments ($1-50): batches per day, threshold $5 minimum, idempotency token per payout; replaces Stripe Connect KYC overhead",
     "lines": 280, "priority": "P0"},
    {"id": "G223", "title": "bin/referral_system.py",
     "purpose": "Consumer referral: A invites B with code, B installs + uploads first clip -> A gets $1 bonus credit, viral coefficient tracking; growth driver for B2C",
     "lines": 250, "priority": "P1"},
    {"id": "G224", "title": "bin/in_app_earnings_counter.py",
     "purpose": "Real-time earnings counter inside tray UI: queries /v1/me/earnings, animated counter, this-week vs all-time view, payout-eligible threshold display",
     "lines": 200, "priority": "P0"},
    {"id": "G225", "title": "bin/auto_updater_winsparkle.py",
     "purpose": "Auto-update mechanism (WinSparkle / Squirrel.Mac): polls update server daily, downloads new installer, prompts consumer 'restart to update' or auto-update overnight",
     "lines": 260, "priority": "P0"},
    {"id": "G226", "title": "bin/crash_reporter_sentry.py",
     "purpose": "Sentry-style crash reporter for consumer app: catches Python + Rust + JS exceptions, sends to Sentry with PII-stripping, no consumer action needed",
     "lines": 230, "priority": "P0"},
    {"id": "G227", "title": "docs/CONSUMER_LANDING_COPY.md",
     "purpose": "Marketing site copy rewritten consumer-first: hero (Play Games, Earn Money) + 3-step (download, play, paid) + payout examples + privacy + referral CTA",
     "lines": 240, "priority": "P0"},

    # ─── W26 Clueless-user MVP gate (1 spec) ───────────────────────────────
    # Howard 2026-05-05: 'let a complete novice install + record + done'
    {"id": "G228", "title": "bin/end_to_end_consumer_smoke.py",
     "purpose": "Master smoke test for the consumer install->play->tarball flow: simulates installer run + game launch + 5-min mock-provider trajectory + auto-stop + PRD-compliant tarball at ~/Documents/OysterClips/; gates the v1 release. Asserts 24/24 G165 lint PASS at end",
     "lines": 280, "priority": "P0"},

    # ─── W27 B2C-critical adds (2 specs) — Howard: 'To C 非常重要' ─────────
    {"id": "G229", "title": "bin/fps_overhead_monitor.py",
     "purpose": "Real-time FPS-overhead monitor: measures consumer game FPS with vs without recording; auto-scales encoder bitrate / disables depth track if overhead > 5 percent. Critical: consumers uninstall instantly if their game's framerate drops",
     "lines": 240, "priority": "P0"},
    {"id": "G230", "title": "bin/in_app_clip_status_widget.py",
     "purpose": "System-tray menu widget showing last 5 clips with green PASS / red FAIL badges (from G165 lint result); consumer sees feedback without opening Finder or CLI. Replaces the missing earnings counter as the primary tray feedback signal",
     "lines": 200, "priority": "P0"},

    # ─── W28 Centralized Error Reporting Service (10 specs) ────────────────
    # Howard: '每次跑都有 log 报错, 做成一个服务, 自动报错'.
    # Self-hosted (not Sentry SaaS) because consumer-fleet scale would cost.
    {"id": "G231", "title": "bin/error_reporting_service.py",
     "purpose": "FastAPI service exposing POST /v1/errors {severity, source, user_id, error_class, traceback, context_json}; rate-limit per IP + per user; dedup by sha256(traceback); writes to Postgres",
     "lines": 320, "priority": "P0"},
    {"id": "G232", "title": "bin/error_storage_postgres.py",
     "purpose": "Alembic migration + SQLAlchemy models for errors table: timestamp, severity, source (recorder/cli/test/cluster), user_id, error_class, traceback_text, context_jsonb, sha256_dedup_key indexed; retention 90 days",
     "lines": 220, "priority": "P0"},
    {"id": "G233", "title": "bin/error_dashboard_web.py",
     "purpose": "Single-page admin dashboard at /admin/errors served by service: filter by severity / source / time / user; group by sha256_dedup_key showing top-10 most-frequent recent errors with sample traceback",
     "lines": 350, "priority": "P0"},
    {"id": "G234", "title": "src/oyster_agent_runner/error_client_python.py",
     "purpose": "Auto-installed Python global error capture: sys.excepthook + threading.excepthook + asyncio default handler + atexit; non-blocking POST to G231 with retry; PII-strip helper for tracebacks",
     "lines": 280, "priority": "P0"},
    {"id": "G235", "title": "bin/error_client_rust_recorder.rs",
     "purpose": "Rust panic handler + std::panic::set_hook compiled into recorder.exe; serializes panic_info + backtrace + machine-id (HMAC-rotated, GDPR-safe) and POSTs to G231 endpoint",
     "lines": 240, "priority": "P0"},
    {"id": "G236", "title": "bin/error_client_pytest_plugin.py",
     "purpose": "pytest plugin (entry-point pytest11) auto-reporting test failures + tracebacks to G231 with source=test, ci_run_id, commit_sha; opt-out via OYSTER_NO_ERROR_REPORT env var",
     "lines": 200, "priority": "P0"},
    {"id": "G237", "title": "bin/auto_install_error_handler.py",
     "purpose": "One-line bootstrap: import this at any process startup -> installs G234 global Python hooks; works via PYTHONSTARTUP / sitecustomize.py / explicit import; zero-config for any runtime",
     "lines": 150, "priority": "P0"},
    {"id": "G238", "title": "docs/runbooks/ERROR_SERVICE_DEPLOY.md",
     "purpose": "Deploy runbook for the error reporting service: Render or Railway + Postgres + env vars + alembic migrate + healthcheck + first end-to-end test (POST a fake error, see it in dashboard)",
     "lines": 250, "priority": "P0"},
    {"id": "G239", "title": "bin/error_severity_classifier.py",
     "purpose": "Auto-classifier: maps incoming error_class + module + traceback signature to severity (critical = data loss / payment / auth; high = crash; medium = degraded; low = warning); rules table with override path",
     "lines": 220, "priority": "P0"},
    {"id": "G240", "title": "bin/error_alert_router.py",
     "purpose": "For critical-severity errors: rate-limited Slack / Discord webhook (configurable); dedup window 5 min per sha256_key; daily-summary digest for medium / low; replaces Sentry pager-style alerts",
     "lines": 200, "priority": "P0"},

    # ─── W29 v1 release readiness (12 NEW-FILE specs) ──────────────────────
    # Howard: 继续推进; closes the consumer-MVP release loop.
    {"id": "G241", "title": "bin/code_signing_windows_authenticode.py",
     "purpose": "Automate Windows Authenticode signing of .msi installer + recorder.exe via signtool.exe in CI; without code signing SmartScreen blocks consumer install with scary warning",
     "lines": 220, "priority": "P0"},
    {"id": "G242", "title": "bin/macos_notarization.py",
     "purpose": "Automate Apple Developer ID notarization workflow for .pkg installer + .app: codesign + altool / notarytool submit + staple; without it Gatekeeper blocks consumer install",
     "lines": 240, "priority": "P0"},
    {"id": "G243", "title": "bin/release_builder_consumer.py",
     "purpose": "End-to-end consumer release builder: takes a tag, builds signed Windows .msi + macOS .pkg, generates SHA256 manifest, uploads to update server, creates GitHub release with assets",
     "lines": 320, "priority": "P0"},
    {"id": "G244", "title": "docs/CONSUMER_TRUST_PAGE.md",
     "purpose": "Public-facing 'what is this software' trust page accessible from system tray menu: what we record, what we do not, anti-cheat statement, privacy summary, GitHub link, contact",
     "lines": 250, "priority": "P0"},
    {"id": "G245", "title": "bin/anonymous_first_run.py",
     "purpose": "Anonymous mode: consumer can install + record + accumulate clips locally without ANY signup or email; opt-in to account later via in-app prompt; clips queued locally, uploaded only after opt-in",
     "lines": 260, "priority": "P0"},
    {"id": "G246", "title": "bin/disk_space_manager.py",
     "purpose": "Auto-cleanup old local clips: configurable cap (default 5 GB), LRU deletion of uploaded clips first, never deletes pending-upload; warns user at 80 percent fill",
     "lines": 230, "priority": "P0"},
    {"id": "G247", "title": "bin/network_throttle_aware.py",
     "purpose": "Detect metered / cellular / paid networks via Windows NetworkInformation API + macOS NWPath; pause uploads on metered connections by default; configurable in tray menu",
     "lines": 200, "priority": "P0"},
    {"id": "G248", "title": "bin/graceful_shutdown_handler.py",
     "purpose": "Hook OS shutdown signals (Win SetConsoleCtrlHandler / macOS SIGTERM via launchd); flush in-flight clip writes, close tarballs atomically, persist queue state; restart-resume-able",
     "lines": 220, "priority": "P0"},
    {"id": "G249", "title": "bin/battery_aware_pause.py",
     "purpose": "On laptops, detect battery vs AC via psutil + IOPSCopyPowerSourcesInfo; pause recording on battery (default) to preserve laptop; configurable override per-game",
     "lines": 180, "priority": "P1"},
    {"id": "G250", "title": "bin/update_server_endpoint.py",
     "purpose": "FastAPI /api/updates/check?platform=windows&current=1.0.0 endpoint serving latest version + download URL + SHA256 + release notes; backs G225 auto-updater",
     "lines": 250, "priority": "P0"},
    {"id": "G251", "title": "bin/version_compatibility_check.py",
     "purpose": "Detect game version at launch (Minecraft 1.20.4 vs 1.21.x): if unsupported, system tray notifies user 'this version unsupported, recording paused'; recommends supported version range",
     "lines": 220, "priority": "P0"},
    {"id": "G252", "title": "bin/i18n_zh_en_strings.py",
     "purpose": "Internationalization: zh-CN + zh-TW + en-US runtime string loader; covers tray menu, splash, privacy dashboard, error messages; .po format compatible with future locale adds",
     "lines": 240, "priority": "P1"},

    # ─── W30 EPal integration (6 NEW-FILE specs) ───────────────────────────
    # Howard 2026-05-05: 'EPal 通过我们的游戏陪玩社群来录这些数据'.
    # See docs/EPAL_INTEGRATION_STRATEGY.md for the lifecycle diagram.
    {"id": "G253", "title": "bin/epal_session_lifecycle_hook.py",
     "purpose": "POST /v1/epal/session_start + /v1/epal/session_end webhooks: recorder starts only on paid EPal session start (with companion opt-in), stops + uploads on session end with rating + companion_id provenance",
     "lines": 280, "priority": "P0"},
    {"id": "G254", "title": "bin/epal_companion_quality_score.py",
     "purpose": "Wire EPal companion professional rating (1-5 stars) into clip metadata; buyer pays premium for higher-rated companion captures; informs cluster training-time data weighting",
     "lines": 200, "priority": "P0"},
    {"id": "G255", "title": "bin/epal_payout_passthrough.py",
     "purpose": "Bonus payout rides EPal payment rails (POST <epal-api>/v1/companion/bonus): no Stripe / PayPal / W-9 — companion sees EPal session pay + recording bonus on one statement",
     "lines": 240, "priority": "P0"},
    {"id": "G256", "title": "bin/epal_client_consent_handshake.py",
     "purpose": "At EPal session start, prompts the paying client (the customer): consent to recording for AI training, opt-out switch, GDPR-compliant disclosure; persists signed consent log per G221",
     "lines": 220, "priority": "P0"},
    {"id": "G257", "title": "bin/epal_community_dashboard.py",
     "purpose": "EPal-side companion dashboard widget: shows clips contributed this week, total bonus earned, leaderboard rank within community; embedded in EPal companion app via iframe / native view",
     "lines": 280, "priority": "P1"},
    {"id": "G258", "title": "docs/EPAL_COMPANION_ONBOARDING.md",
     "purpose": "Companion-facing onboarding doc: how to enable recording in EPal companion app, what gets recorded vs not, FAQ on anti-cheat, bonus calc, opt-out anytime",
     "lines": 220, "priority": "P0"},
]


def build_entries() -> list[dict]:
    """Build gap entries from SPECS with default values.

    Converts each spec in SPECS into a gap entry dict with
    default 'status' set to 'pending'.

    Returns:
        List of gap entry dicts with keys: id, title, purpose,
        status, priority, lines_estimate.
    """
    out = []
    for s in SPECS:
        entry = {
            "id": s["id"],
            "title": s["title"],
            "purpose": s["purpose"],
            "status": "pending",
            "priority": s["priority"],
            "lines_estimate": s["lines"],
        }
        out.append(entry)
    return out


def main(apply: bool = False) -> int:
    """Generate or apply new audit gap specs to docs/audit_gaps.yaml.

    Args:
        apply: If False, performs a dry-run that prints what would be added
            without modifying the file. If True, actually writes new gap entries.

    Returns:
        int: Exit code (0 for success, non-zero for errors).

    Example:
        >>> spec_generator.main()          # dry-run
        >>> spec_generator.main(apply=True)  # actually write
    """
    data = yaml.safe_load(GAPS_FILE.read_text())
    existing_ids = {g["id"] for g in data.get("gaps", [])}
    new_entries = [e for e in build_entries() if e["id"] not in existing_ids]

    print(f"existing gaps: {len(data.get('gaps', []))}")
    print(f"new specs to add: {len(new_entries)} (skipping {len(SPECS) - len(new_entries)} dupes)")
    if not new_entries:
        print("nothing to add")
        return 0

    print("first 3 new entries (preview):")
    for e in new_entries[:3]:
        print(f"  {e['id']}: {e['title']}  ({e['priority']})")
    print(f"  ... and {len(new_entries) - 3} more")

    if not apply:
        print("\n(dry-run; pass --apply to write)")
        return 0

    data.setdefault("gaps", []).extend(new_entries)
    # safe_dump avoids the manual quoting hell that broke the file last time.
    out = yaml.safe_dump(data, sort_keys=False, allow_unicode=True, width=120)
    GAPS_FILE.write_text(out)

    # Re-validate immediately so we never push broken YAML.
    yaml.safe_load(GAPS_FILE.read_text())
    print(f"\nwrote {GAPS_FILE} successfully ({len(data['gaps'])} gaps total)")
    return 0


if __name__ == "__main__":
    sys.exit(main(apply="--apply" in sys.argv))
